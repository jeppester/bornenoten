<%= form_for(@child) do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :date_of_birth %>
  <%= f.text_area :goals %>
  <%= f.select :employee_id, [['Ingen valgt', '']] + Employee.all.pluck(:name, :id) %>
  <%= f.file_field :portrait %>
  <%= f.submit @child.new_record? ? 'Opret barn' : 'Gem ændringer' %>
<% end %>
