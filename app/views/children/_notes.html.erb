<div class="note-list">
  <% [*notes, 'new'].each_slice(4) do |row_notes| %>
    <div class="note-list__row">
      <% row_notes.each do |note| %>
        <div class="note-item">
          <% if note != 'new' %>
            <div class="note-item__actions">
              <%= link_to edit_child_note_path(note.child, note) do %>
                <%= fa_icon "pencil" %>
              <% end %>
              <%= link_to child_note_path(note.child, note), method: :delete, data: { confirm: 'Slet note?' } do %>
                <%= fa_icon "trash" %>
              <% end %>
            </div>
            <div class="note-item__content">
              <%= simple_format(note.content) %>
            </div>
          <% else %>
            <div class="note-item__content note-item__content--new">
              <%= fa_icon 'plus' %>
              <h2>
                Opret en ny note
              </h2>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
